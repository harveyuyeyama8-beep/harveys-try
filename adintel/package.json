{
  "name": "adintel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Competitor ad intelligence pipeline for Harvey's Coffee",
  "bin": {
    "adintel": "./cli.js"
  },
  "scripts": {
    "run": "node cli.js run",
    "demo": "node cli.js run --demo",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0",
    "@typesafe-ai/sdk": "^0.6.0",
    "zod": "^4.6.5"
  }
}
