{
  "name": "harveys-try",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Advertorial landing pages for Harvey's Coffee — try.harveyscoffee.shop",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=harveys-try"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "marked": "^15.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.90.0"
  }
}
